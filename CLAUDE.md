# Project Guidelines

## Icons
- Use **`react-icons/md`** (Material Design) for all icons — do NOT use `lucide-react`
- Import pattern: `import { MdIconName } from "react-icons/md"`
- Icon type for props: `import type { IconType } from "react-icons"`
